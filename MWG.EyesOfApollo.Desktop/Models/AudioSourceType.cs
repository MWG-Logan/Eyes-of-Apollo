namespace MWG.EyesOfApollo.Desktop.Models
{
    /// <summary>
    /// Defines the audio source to visualize.
    /// </summary>
    public enum AudioSourceType
    {
        /// <summary>
        /// Capture from an input device (microphone/line-in).
        /// </summary>
        Input,
        /// <summary>
        /// Capture from an output device (system playback).
        /// </summary>
        Output
    }
}
